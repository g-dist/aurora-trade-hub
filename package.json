{
  "name": "aurora-trade-hub",
  "private": true,
  "version": "0.0.1",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:portal": "npm run dev --workspace=apps/portal",
    "dev:jessica": "npm run dev --workspace=apps/jessica",
    "build": "npm run build --workspaces",
    "build:portal": "npm run build --workspace=apps/portal",
    "build:jessica": "npm run build --workspace=apps/jessica"
  }
}
